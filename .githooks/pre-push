#!/usr/bin/env pwsh
$ErrorActionPreference = "Stop"

Write-Host "Running Mahmoud AI pre-push quality gate..."
pwsh -NoProfile -File eng/verify.ps1
if ($LASTEXITCODE -ne 0) {
    Write-Host "Pre-push quality gate failed. Push aborted." -ForegroundColor Red
    exit 1
}
exit 0
